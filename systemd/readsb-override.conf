[Unit]
# No hard dependency — readsb must not block boot if SDR is absent
After=network.target

[Service]
Restart=on-failure
RestartSec=30s
# Retry indefinitely until hardware appears
StartLimitIntervalSec=0
